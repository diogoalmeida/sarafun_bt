# This file defines where is a particular subtree for a specific keyframe label
initial:
  file: initial.json

grasp:
  file: grasp_parallel.json

pick_up:
  file: pick.json

align:
  file: align.json

contact:
  file: contact.json

assembled:
  file: assembled.json

retract:
  file: retract.json

contact:
  file: contact.json

move:
  file: move.json

insert:
  file: insert.json
  
deform:
  file: deform.json  
